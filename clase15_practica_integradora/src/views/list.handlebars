<div>
    {{#each pokemons }}
        <div>
            <a href="#">
                <b>{{this.name}}</b>
                <img src="{{this.photo}}" height="50">
                <i>{{this.type}}</i>
            </a>
        </div>
    {{/each}}
</div>